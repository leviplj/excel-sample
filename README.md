Configurando o virtual env usando python 3+:

`python -m venv .venv`

Ativando o vitual env:

Linux: `source .venv/bin/activate`
Windows: `.\env\Scripts\activate.bat`

Instalando dependências:

`pip install openpyxl`